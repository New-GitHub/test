pipeline {
   agent any
   stages {
      stage ('Example') {
         steps {
            echo "Running ${env.BUID_NUMBER} on ${env.JENKINS_URL}"
            echo "Running ${BUID_NUMBER} on ${JENKINS_URL}"
            echo "Running $env.BUID_NUMBER on $env.JENKINS_URL"
         }
      }
   }
}